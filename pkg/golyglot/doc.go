// Package golyglot provides a native Go SQL parser, generator, formatter,
// builder, validator, query analyzer, lineage engine, and AST visitor toolkit.
// It is deliberately independent of cgo, WASM, FFI, and language-server
// protocol packages so it can be embedded directly by Go applications.
package golyglot
